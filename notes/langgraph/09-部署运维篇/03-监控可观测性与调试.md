# 监控、可观测性与调试

生产环境中，可观测性是保障系统可靠性的关键。本篇介绍 LangSmith 集成、日志系统、性能监控、告警体系。

---

## 可观测性三大支柱

```
┌─────────────────────────────────────────────┐
│            可观测性三大支柱                    │
├──────────────┬──────────────────────────────┤
│              │                              │
│  日志        │  记录离散事件                 │
│  (Logging)   │  "什么时间发生了什么"          │
│              │                              │
├──────────────┼──────────────────────────────┤
│              │                              │
│  指标        │  聚合数值                     │
│  (Metrics)   │  "QPS、延迟、错误率"          │
│              │                              │
├──────────────┼──────────────────────────────┤
│              │                              │
│  追踪        │  请求级链路                   │
│  (Tracing)   │  "一个请求经历了哪些步骤"      │
│              │                              │
└──────────────┴──────────────────────────────┘
```

---

## LangSmith 深度集成

### 配置

```python
import os

os.environ["LANGSMITH_TRACING"] = "true"
os.environ["LANGSMITH_API_KEY"] = "your-key"
os.environ["LANGSMITH_PROJECT"] = "production"
os.environ["LANGSMITH_ENDPOINT"] = "https://api.smith.langchain.com"
```

### 追踪内容

每次 `graph.invoke()` 会自动记录：

| 数据 | 说明 |
|------|------|
| 节点执行顺序 | 每个节点的调用链 |
| 输入/输出 | 每个节点的完整数据 |
| LLM 调用 | Prompt、Response、Model、Token |
| 工具调用 | 工具名、参数、结果、耗时 |
| 状态快照 | 每步的状态值 |
| 错误信息 | 异常堆栈 |
| 性能指标 | 各步耗时 |

### 添加自定义元数据

```python
result = graph.invoke(
    {"messages": [("user", "你好")]},
    config={
        "configurable": {"thread_id": "user-123"},
        "metadata": {
            "user_id": "user-123",
            "session_type": "premium",
            "experiment": "v2.1",
            "tags": ["production", "rag-agent"]
        },
        "tags": ["production"]
    }
)
```

### 查询追踪数据

```python
from langsmith import Client

client = Client()

# 获取最近的运行
runs = client.list_runs(
    project_name="production",
    limit=10,
    execution_order=1  # 只看顶层运行
)

for run in runs:
    print(f"Run ID: {run.id}")
    print(f"  Name: {run.name}")
    print(f"  Status: {run.status}")
    print(f"  Start: {run.start_time}")
    print(f"  End: {run.end_time}")
    print(f"  Tokens: {run.total_tokens}")
    print()
```

---

## 日志系统

### 结构化日志

```python
import logging
import json
from datetime import datetime

class StructuredFormatter(logging.Formatter):
    """JSON 格式日志"""

    def format(self, record):
        log_data = {
            "timestamp": datetime.utcnow().isoformat(),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
            "module": record.module,
            "line": record.lineno,
        }

        if hasattr(record, 'extra_data'):
            log_data.update(record.extra_data)

        if record.exc_info:
            log_data["exception"] = self.formatException(record.exc_info)

        return json.dumps(log_data, ensure_ascii=False)

# 配置
logger = logging.getLogger("langgraph_app")
logger.setLevel(logging.INFO)

handler = logging.StreamHandler()
handler.setFormatter(StructuredFormatter())
logger.addHandler(handler)
```

### 在节点中使用

```python
def my_node(state: State) -> dict:
    logger.info(
        "Node executed",
        extra={"extra_data": {
            "node": "my_node",
            "messages_count": len(state.get("messages", [])),
            "user_id": state.get("user_id", "unknown")
        }}
    )
    ...
```

---

## 性能指标监控

### 自定义指标收集

```python
import time
from collections import defaultdict
from dataclasses import dataclass, field

@dataclass
class MetricsCollector:
    """指标收集器"""
    counters: dict = field(default_factory=lambda: defaultdict(int))
    histograms: dict = field(default_factory=lambda: defaultdict(list))

    def increment(self, name: str, value: int = 1):
        self.counters[name] += value

    def record_time(self, name: str, duration: float):
        self.histograms[name].append(duration)

    def get_stats(self) -> dict:
        stats = {}
        for name, values in self.histograms.items():
            if values:
                stats[name] = {
                    "count": len(values),
                    "avg": sum(values) / len(values),
                    "min": min(values),
                    "max": max(values),
                    "p50": sorted(values)[len(values)//2],
                    "p99": sorted(values)[int(len(values)*0.99)] if len(values) > 100 else max(values),
                }
        return {**dict(self.counters), **stats}

metrics = MetricsCollector()
```

### 在图中使用

```python
def instrumented_node(state: State) -> dict:
    """带性能监控的节点"""
    start = time.time()

    try:
        result = actual_processing(state)
        metrics.increment("node_success")
        return result
    except Exception as e:
        metrics.increment("node_error")
        raise
    finally:
        elapsed = time.time() - start
        metrics.record_time("node_duration", elapsed)
```

### 关键指标

```python
# 定义需要监控的指标
METRICS = {
    # 请求级
    "request_total": "总请求数",
    "request_success": "成功请求数",
    "request_error": "失败请求数",

    # 延迟
    "request_duration": "请求总耗时",
    "llm_duration": "LLM 调用耗时",
    "tool_duration": "工具执行耗时",

    # Token
    "prompt_tokens": "输入 Token 数",
    "completion_tokens": "输出 Token 数",

    # 业务
    "tool_calls_total": "工具调用总次数",
    "recursion_depth": "平均递归深度",
}
```

---

## 告警体系

### 错误告警

```python
import smtplib
from email.mime.text import MIMEText

class AlertManager:
    def __init__(self, config):
        self.config = config

    def send_alert(self, level: str, title: str, message: str):
        """发送告警"""
        # 邮件
        if level in ["CRITICAL", "HIGH"]:
            self._send_email(title, message)

        # Webhook（Slack/钉钉等）
        self._send_webhook(level, title, message)

    def _send_webhook(self, level, title, message):
        import requests
        webhook_url = self.config["webhook_url"]
        payload = {
            "level": level,
            "title": title,
            "message": message,
            "timestamp": datetime.now().isoformat()
        }
        try:
            requests.post(webhook_url, json=payload, timeout=5)
        except:
            pass  # 告警发送失败不应影响主流程

alerts = AlertManager(config)
```

### 告警规则

```python
class AlertRules:
    """告警规则"""

    @staticmethod
    def check_error_rate(metrics: dict, threshold: float = 0.05):
        """错误率告警"""
        total = metrics.get("request_total", 0)
        errors = metrics.get("request_error", 0)
        if total > 100:  # 至少100个请求才判断
            rate = errors / total
            if rate > threshold:
                alerts.send_alert(
                    "HIGH",
                    "错误率过高",
                    f"当前错误率 {rate:.2%}，超过阈值 {threshold:.2%}"
                )

    @staticmethod
    def check_latency(metrics: dict, threshold: float = 10.0):
        """延迟告警"""
        duration = metrics.get("request_duration", {})
        p99 = duration.get("p99", 0)
        if p99 > threshold:
            alerts.send_alert(
                "MEDIUM",
                "P99延迟过高",
                f"P99 = {p99:.1f}s，超过阈值 {threshold}s"
            )

    @staticmethod
    def check_recursion_limit(metrics: dict):
        """递归深度告警"""
        depth = metrics.get("recursion_depth", 0)
        if depth > 20:
            alerts.send_alert(
                "LOW",
                "递归深度较高",
                f"平均递归 {depth} 步，可能存在循环"
            )
```

---

## 健康检查

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
async def health_check():
    """健康检查"""
    checks = {
        "database": await check_db_connection(),
        "redis": await check_redis_connection(),
        "llm_api": await check_llm_api(),
    }

    all_healthy = all(checks.values())

    return {
        "status": "healthy" if all_healthy else "degraded",
        "checks": checks,
        "timestamp": datetime.now().isoformat()
    }

@app.get("/metrics")
async def get_metrics():
    """Prometheus 格式指标"""
    stats = metrics.get_stats()
    lines = []
    for name, value in stats.items():
        lines.append(f"langgraph_{name} {value}")
    return "\n".join(lines)
```

---

## 小结

| 要点 | 说明 |
|------|------|
| LangSmith | 全链路追踪（强烈推荐） |
| 结构化日志 | JSON 格式便于检索 |
| 性能指标 | QPS、延迟、错误率 |
| 告警体系 | 错误率、延迟、递归深度 |
| 健康检查 | 数据库、Redis、API |
| 自定义元数据 | 便于追踪和过滤 |

---

## 下一篇

➡️ [性能优化与扩展](./04-性能优化与扩展.md)
