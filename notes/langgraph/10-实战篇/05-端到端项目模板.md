# 端到端项目模板

一个可复用的 LangGraph 项目脚手架——包含目录结构、配置、代码模板和最佳实践。

---

## 项目结构

```
my-langgraph-app/
├── .env                    # 环境变量
├── .gitignore
├── requirements.txt
├── pyproject.toml          # 项目配置
├── langgraph.json          # LangGraph 配置
├── README.md
├── src/
│   ├── __init__.py
│   ├── graph.py            # 主图定义
│   ├── state.py            # 状态定义
│   ├── nodes/
│   │   ├── __init__.py
│   │   ├── agent.py        # Agent 节点
│   │   ├── tools.py        # 工具节点
│   │   └── router.py       # 路由逻辑
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── search.py       # 搜索工具
│   │   └── custom.py       # 自定义工具
│   ├── prompts.py          # Prompt 模板
│   └── config.py           # 应用配置
├── tests/
│   ├── __init__.py
│   ├── test_graph.py
│   └── test_tools.py
└── docker-compose.yml
```

---

## state.py

```python
"""状态定义"""
from typing import Annotated
from typing_extensions import TypedDict
from langgraph.graph.message import add_messages
import operator


class AgentState(TypedDict):
    """主状态"""
    # 对话
    messages: Annotated[list, add_messages]

    # 用户信息
    user_id: str

    # 工作数据
    retrieved_docs: Annotated[list, operator.add]
    current_task: str

    # 结果
    answer: str
    confidence: float

    # 元数据
    step_count: int
    error: str
```

---

## graph.py

```python
"""主图定义"""
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import MemorySaver
from langgraph.prebuilt import ToolNode

from .state import AgentState
from .nodes.agent import agent_node, should_continue
from .tools.search import search_tools


def build_graph():
    """构建并编译图"""
    builder = StateGraph(AgentState)

    # 添加节点
    builder.add_node("agent", agent_node)
    builder.add_node("tools", ToolNode(search_tools))

    # 添加边
    builder.add_edge(START, "agent")
    builder.add_conditional_edges("agent", should_continue, {
        "tools": "tools",
        END: END,
    })
    builder.add_edge("tools", "agent")

    # 编译
    graph = builder.compile(
        checkpointer=MemorySaver(),
    )

    return graph


# 全局图实例
graph = build_graph()
```

---

## config.py

```python
"""应用配置"""
import os
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    # LLM
    openai_api_key: str = ""
    model_name: str = "gpt-4o"
    temperature: float = 0.3

    # 应用
    max_recursion: int = 25
    request_timeout: int = 60

    # LangSmith
    langsmith_api_key: str = ""
    langsmith_project: str = "langgraph-app"

    class Config:
        env_file = ".env"


settings = Settings()

# 设置 LangSmith
if settings.langsmith_api_key:
    os.environ["LANGSMITH_TRACING"] = "true"
    os.environ["LANGSMITH_API_KEY"] = settings.langsmith_api_key
    os.environ["LANGSMITH_PROJECT"] = settings.langsmith_project
```

---

## requirements.txt

```
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-tavily>=0.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
```

---

## langgraph.json

```json
{
    "dependencies": ["."],
    "graphs": {
        "agent": "./src/graph.py:graph"
    },
    "env": ".env"
}
```

---

## docker-compose.yml

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: langgraph
      POSTGRES_USER: langgraph
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
```

---

## 启动检查清单

```
□ 1. 创建虚拟环境
  python -m venv venv && source venv/bin/activate

□ 2. 安装依赖
  pip install -r requirements.txt

□ 3. 配置 .env
  OPENAI_API_KEY=sk-...
  LANGSMITH_API_KEY=lsv2_...

□ 4. 本地开发运行
  langgraph dev  (启动 Studio)
  # 或
  uvicorn src.api:app --reload  (API模式)

□ 5. 运行测试
  pytest tests/

□ 6. Docker 部署
  docker-compose up -d

□ 7. 验证
  curl http://localhost:8000/health
```

---

## 生产检查清单

```
□ 安全
  □ API Key 不在代码中
  □ 输入验证和过滤
  □ 速率限制
  □ CORS 配置

□ 可靠性
  □ 状态持久化（PostgreSQL）
  □ 错误处理和重试
  □ 超时设置
  □ 优雅降级

□ 可观测性
  □ LangSmith 追踪
  □ 结构化日志
  □ 性能指标
  □ 告警配置

□ 性能
  □ 缓存策略
  □ 并行化
  □ 流式输出
  □ 成本监控
```

---

## 小结

本模板提供了：
- 标准项目结构
- 配置管理
- 状态定义模板
- 图构建模板
- Docker 部署
- 启动和生产检查清单

---

## 下一篇

➡️ 前往 [11-附录](../11-附录/01-速查表-CheatSheet.md)
